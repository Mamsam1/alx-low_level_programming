#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char s;

	int t;

	s = 'a';
	t = 0;
	while
		(t < 10) {
			putchar(t + 0);
			t++;
		}
	while
		(s <= 'v') {
			putchar(s);
			s++;
		}
	putchar('\n');
	return (0);
}
