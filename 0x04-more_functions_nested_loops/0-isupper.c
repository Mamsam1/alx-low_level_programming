#include "main.h"

/**
  * _isupper - upper case check
  * @c: The number to be checked
  *
  * Return: 1 or 0
  */
int _isupper(int c)
{
	if (c >= 'A' && x <= 'Z')
	{
		return (1);
	}

	return (0);
}
